rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    // Allow everything in emulator; lock down in prod later
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
